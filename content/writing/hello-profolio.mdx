---
title: "Hello Profolio"
description: "上传脚本连通性测试：MDX 渲染与 Storage 拉取。"
tag: "本站"
file_path: "hello-profolio.mdx"
---

## 小节标题

这是一段**粗体**与 `inline code` 的示例。

### 列表示例

- 第一项
- 第二项

```ts
export function greet(name: string) {
  return `Hello, ${name}!`;
}
```

> 引用块：终端建筑师风格下的一段说明。

[返回分享列表](/writing)
